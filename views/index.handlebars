<div class="burgers">
    <div>
        <img class="peter-pepper" src="../public/assets/img/BurgerTimeDudeNoBG L.png" alt="Peter Pepper">
    </div>
    <div class="left-side">
        <h2>To Be Devoured</h2>
        <ul>
            {{#each burgers}}
            {{#unless devoured}}
            <li class="burger-list">{{burger_name}} <button class="tbDevoured button" data-id="{{id}}"
                    data-burger="{{devoured}}">DEVOUR</button></li>
            {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="right-side">
        <h2>Devoured</h2>
        <ul>
            {{#each burgers}}
            {{#if devoured}}
            <li class="burger-list">{{burger_name}} <button class="tbPuke button" data-id="{{id}}" data-burger="{{devoured}}">UP CHUCK</button></li>
            {{/if}}
            {{/each}}
        </ul>
    </div>
    <div>
        <img class="peter-pepper" src="../public/assets/img/BurgerTimeDudeNoBG R.png" alt="Peter Pepper">
    </div>
</div>

<div class="form">
    <form class="create-form">
        <div class="form-group form-stuff">
            <label for="new-burger">Burger Name:</label>
            <input class="input" type="text" id="new-Burger" name="name" placeholder="Enter new burger here">
        </div>
        <button class="button" type="submit">WELCOME TO FLAVOR TOWN</button>
    </form>
</div>

<div class="bottom-image">
    <img class="actual-b-image" src="../public/assets/img/AllThree.png" alt="Burger Time Characters">
</div>